# Create a file in the backend folder with the name .env
# Then copy the lines of code and update them with your data

DB_HOST=tcp://127.0.0.1:3306
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_database


#Change origin to the domain once it's on production phase
ALLOWED_ORIGIN=http://localhost:5173